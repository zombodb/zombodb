[package]
name = "pg-bridge"
version = "0.1.0"
authors = ["Eric B. Ridge <eebbrr@gmail.com>"]
edition = "2018"

[dependencies]
pg-guard-attr = { path = "pg-guard-attr/" }
pg-guard = { path = "pg-guard" }
