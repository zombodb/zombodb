[workspace]
members = [
    "zdb-helper",
    "pg-bridge",
    "pg-bridge/pg-guard-attr",
    "pg-bridge/pg-guard-rewriter",
    "pg-bridge/pg-guard"
]

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
opt-level = 3
lto = true
codegen-units = 1