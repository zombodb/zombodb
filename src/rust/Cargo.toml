[workspace]
members = [
    "zdb-helper",
    "pg-bridge",
    "pg-bridge/pg-guard-attr",
    "pg-bridge/pg-guard",
    "pg-bridge/pg-sys",
]

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
opt-level = 3
lto = true
codegen-units = 1